export type postData = {
    selectedNumbers: number[],
    numbersPerTicket: number,
    condition: number,
    guarantee: number,
    even: number,
    odd: number,
    collum: number,
    line: number,
    sequencial: number,
}